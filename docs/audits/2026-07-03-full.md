# Audit report — 2026-07-03 (full development: ch. 0–5, GLOSSARY, INTAKE, DR-0001..0006, all Lean, positioning note, paper draft)

Auditor stance: hostile referee. Evidence gathered this session: fresh `lake build`
(1995 jobs, exit 0, zero warnings), `sorry`/`admit` grep (0 hits outside a comment),
existence check of **all 50** doc-cited Lean declarations (all present), banned-word
scan, status-label scan of every numbered item (61 items), stale-status cross-check of
INTAKE vs chapters, cross-reference spot checks, architectural-contract check (no ch. 5
citation in ch. 1–4 — grep hits are all D2-source numbering).

## Critical (soundness of the system at risk)

*None found.* The Lean development backing every `[VERIFIED]` label builds clean and
sorry-free; the completeness chain (Thm 4.13 → 4.14 → 4.16) was re-checked against the
`Closes`-encoding deviation note, which is honestly recorded (Thm 4.13 note, DR-0005).

## Major (a label, proof, or sync is wrong)

- **M1. `papers/npl-core/main.tex:35` — `\newcommand{\let}` redefines a TeX primitive.**
  `\let` is core TeX; `\newcommand` on it is an error, so the draft does not compile at
  all. Why it matters: the deliverable is dead on arrival. Fix: rename the macro (used
  at lines 209, 211). **Applied: renamed to `\tle`.**
- **M2. `docs/INTAKE.md` §E rows 86–87 — stale statuses contradicting the chapters.**
  Row "Soundness of ND system … `[CONJECTURE]`" (resolved as Prop 3.7–3.9 `[VERIFIED]`)
  and row "FDE conservativity … `[CONJECTURE]` … needs citation" (C5 is `[PROVEN]`,
  citations installed). Why it matters: INTAKE is the migration ledger; a wrong ledger
  entry is exactly the kind of drift R1/R7 exist to prevent. **Applied: rows updated**
  (the conservativity row now points at the new Cor 4.20, see follow-ups).

## Minor (hygiene)

- **m1. `docs/04-metatheory.md` Def 4.9 — the only numbered item with no status label**
  (61 items scanned; 60 correct). Fix: label `[DRAFT]` per the definition convention.
  **Applied.**
- **m2. `docs/WORKFLOW.md` layout table ends at `04-metatheory.md`** — chapter 5 exists
  since today. **Applied: row added.**
- **m3. `Nullivance/Nullivance/Basic.lean` is leftover scaffolding** (`def hello :=
  "world"`), imported by the root module — harmless but unprofessional in a published
  artifact. **Applied: replaced by a library docstring module.**
- **m4. "trivialize" in `docs/00-motivation.md` (D4).** Caught by the R6 scan; judged
  acceptable — it is not a proof-justification word, and chapter 0 is the designated
  informal chapter. No action; recorded so the scan stays honest.
- **m5. Paper: `plainurl` bibstyle + author placeholder.** Already tracked in
  `papers/npl-core/README.md` pre-submission checklist. No new action.

## Observations (not defects; suggestions — three applied as follow-ups)

- **O1. Definitions never leave `[DRAFT]`.** R1 has no terminal status for definitions
  (they are not provable). All 20 definition items are `[DRAFT]` forever under current
  rules. Recommend a DR amending R1/WORKFLOW with a definition-only terminal status
  (e.g. `[STABLE]` granted by audit). *Left for the user — rule change is theirs (R4 spirit).*
- **O2. Consistency evidence was implicit only** (checklist B's top item): countermodels
  existed in Lean (`vBN`, `vTF`) but no numbered statement said "the consequence
  relation is nontrivial". **Applied as Prop 4.21** (∅ ⊬ T⁺p, with Lean witness) —
  the axiom base now has recorded model evidence.
- **O3. ∨ is value-definable from {¬,∧}** (Lem 2.9(iii)); keeping it primitive is a
  presentation choice, standard for FDE. Recommend a one-line remark in ch. 1. *Left.*
- **O4. ⊕ is NOT definable from {¬,∧,∨}** — the classical-corner argument: every
  {¬,∧,∨}-term maps classical inputs to classical values, but T⊕F = N. This justifies
  ⊕ as a primitive and strengthens the paper's story. **Applied as Prop 4.22 (Lean:
  `Metatheory.classical_closed`, `oplus_not_definable`).**
- **O5. D1 Thm 5 (FDE conservativity on {¬,∧,∨}) now follows from C5** + identity of
  designated sets. **Applied as Cor 4.20**, closing INTAKE §E row 87 properly.
- **O6. σ, ρ, δ (INTAKE §A blocked row):** confirmed excised — no occurrence anywhere in
  `docs/` outside the INTAKE record itself. Keep the record.
- **O7. Standing open checks, correctly tracked:** Arieli–Avron bifilter-collapse vs
  unsigned consequence (positioning §2); compactness (INTAKE §F.2); ND completeness
  (§F.1). All three are stated in the paper draft as open — no overclaim found.

## Verdict

No critical findings; two major (one compile-blocking in the paper, one ledger drift),
both fixed this session; label discipline otherwise exact (60/61 before fixes). The
`[VERIFIED]` labels are all backed by the current build. The development is in a
submittable state once the paper-README checklist items (author name, bifilter check,
literature sweep) are discharged.
