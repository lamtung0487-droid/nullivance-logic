# NPL finite-domain quantified manuscript

Release v0.7 (2026-08-16). This is the companion to the propositional manuscript.

`main.tex` presents the finite-domain quantified semantics, the macro-free core
extensional tableau and its verified completeness theorem, the constrained
grounding corollary, the counterexamples to the older head-sensitive replay
certificates, and the verified membership-selecting repair. Evidence markers
`[L]` name Lean declarations checked by the release gate.

Contact: `lamtung0481@gmail.com`.

Build from the repository root:

```powershell
& ./scripts/Build-Papers.ps1 -Paper npl-finite-fo
```

The bibliography is shared at `references/bibliography.bib`. The checked-in
`main.pdf` is generated by the same release build gate.
