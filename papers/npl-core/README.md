# NPL core manuscript

Release v0.7 (2026-08-16).

`main.tex` presents the propositional two-channel semantics, exact threshold
projection to FOUR, the four-signed analytic tableau, the verified soundness and
completeness results, compactness, and terminating operational proof search.
Evidence markers `[L]` name declarations checked by
`scripts/Verify-ManuscriptLeanRefs.ps1`; `[paper]` marks paper-only arguments.

Contact: `lamtung0481@gmail.com`.

Build from the repository root:

```powershell
& ./scripts/Build-Papers.ps1 -Paper npl-core
```

The bibliography is shared at `references/bibliography.bib`. The checked-in
`main.pdf` is generated by the same release build gate.
