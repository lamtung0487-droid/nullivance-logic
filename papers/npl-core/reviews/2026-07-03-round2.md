# Panel review, round 2 — same manuscript, draft v0.4 (2026-07-03)

Delta review: each referee verifies the disposition of their round-1 major comments,
then makes a fresh pass. Manuscript: `papers/npl-core/main.tex` @ commit c752776.

---

# Referee 1 report — Correctness & rigor (round 2)

**Recommendation: MINOR REVISION** · Confidence: high

Round-1 majors: all resolved. (1.1 stale remark — deleted, replaced by nothing false;
1.2 tableau rules — full table now printed and checked cell-by-cell against the
development's Def 3.3: all sixteen entries match, including the ⊕ row's conjunctive
F⁺; 1.3 ND rules — explicit list printed, matches the 16 Lean constructors; 1.4
compactness — full proof now printed, countability invoked, choice usage stated; 1.5
⊨_FOUR — defined; 1.6 marker coverage — [L]/[paper] split introduced and spot-checked
(decidability now [paper] with the lemma correctly attributed; FDE-conservativity half
marked [paper]); 1.7 proof sketches — present on every previously bare result, each
sufficient for an expert to reconstruct.)

## Remaining comments (minor unless noted)

1. **[Major-but-local] Theorem 6.4(iii), citation slip.** The text reads "if no finite
   subset derives, then by Theorem 6.2 [complete-cont] no finite
   $\Sigma_0\models_{\FOUR}S\varphi$". Theorem 6.2 relates $\dershort$ to the
   *continuous* $\models$; the $\models_{\FOUR}$ step is Theorem 6.1. The argument is
   correct with the right citation; as printed the step cites the wrong theorem. Fix:
   cite Theorem 6.1 (or "6.1/6.2").
2. Definition 5.1 lists only two of the four opposite-sign pairs
   ($\overline{\Tp}=\Tm$, $\overline{\Fp}=\Fm$); add the converses or say "and
   vice versa".
3. Proposition 3.4's proof sketch forward-references §6 machinery. Acceptable with
   the explicit pointer, but consider stating it after Theorem 6.2 and keeping only
   the statement in §3.
4. `def:four` carries two labels (`def:four`, `def:fourcons`) on one environment;
   cross-references resolve to the same number — harmless, but one label suffices.

Certification: the new Proposition 3.4 (τ-invariance) was checked against
`consequenceCAt_iff_consequenceC` — statement matches, including the (0,1] bounds.
The compactness proof was re-derived line by line; the pigeonhole and the
finite-dependence steps are correct and complete. No degenerate-case failures found
in the new material.

---

# Referee 2 report — Novelty & positioning (round 2)

**Recommendation: MINOR REVISION** · Confidence: high

Round-1 majors: resolved in substance. (2.1 bifilter — now Proposition 9.1, a real
two-part claim replacing the hedge; the subsumption direction is admitted plainly and
cross-checked against the paper's own completeness route; 2.2 reduction attack — the
contribution list now leads with the signed-layer separation; 2.3 Fitting — engaged
with the correct distinction (fixed-point program semantics vs consequence +
completeness); 2.4 §8 — recentered on the import-graph point; 2.5 neologism —
footnoted.)

## Remaining comments

1. **Attribution hygiene on the collapse theorem.** Proposition 9.1 cites
   \cite[Thm.~2.17]{arieli1996reasoning}, but the project's own audit trail records
   that the theorem was verified through Rivieccio's presentation, not the primary
   text. Either verify against the primary or cite it as "as presented in
   \cite{rivieccio2010algebraic}". Scholarly honesty is this paper's brand; protect it.
2. **The post-2022 sweep is still open and now the paper's weakest disclosure.** In
   particular, the authors of \cite{bilkova2022paraconsistent} have an adjacent line
   on *tableaux for two-dimensional fuzzy logics* that a referee will know; the sweep
   must find and engage it before submission.
3. Suggestion (not blocking): the separation in Proposition 9.1(ii) invites a
   companion *inexpressibility* fact — no formula $\psi(p)$ makes $\Tp\psi$ equivalent
   to $\Tm p$ (the constant-$\vB$ valuation is a fixpoint of all connectives, so
   definable designation is stuck at $\vB$). One paragraph would preempt the "just
   internalize the negative signs" objection. [Editor note: worth doing — it is a
   three-line argument in the paper's own style.]

---

# Referee 3 report — Presentation & scholarship (round 2)

**Recommendation: MINOR REVISION** · Confidence: high

Round-1 majors: resolved. (3.1 artifact statement — present with toolchain, build
command, coverage policy; 3.2 residue — version footnote and "fix before submission"
gone; 3.3 bibliography — `plain` is safe, `\mathbb{1}` eliminated in favor of a
defined indicator macro; 3.4 §7 retitled.)

## Remaining comments

1. **Author field still reads `[author name to be confirmed]`** — by design at draft
   stage, but it is the last piece of visible scaffolding.
2. Abstract is ~290 words; several target venues cap at 250. Trim the ingredient
   recital (the positioning paragraph of §1 carries it).
3. `plain` bibliography style will render the `note` fields of the two `@misc`/annotated
   entries verbatim, producing long entries (the Bílková and Rivieccio notes include
   project-internal cross-references like "see references/npl-positioning.md §3").
   **Strip project-internal paths from .bib notes before submission** — they leak
   internal file structure into the published bibliography.
4. Minor English: "the epistemic state of a proposition has two independent
   coordinates" — "state … has coordinates" is fine, but the abstract's parallel
   sentence uses "read as" twice in three lines.

---

# Editor meta-review (round 2)

**Decision: MINOR REVISION.** All round-1 blocking items are verifiably resolved; the
remaining list is short, local, and contains exactly one mathematical defect (a wrong
cross-reference inside a correct proof).

## Revision checklist (round 2)

1. Fix the Theorem 6.4(iii) citation (6.2 → 6.1). (R1.1)
2. Cite the collapse theorem via its verified route: primary number + "as presented
   in" the secondary. (R2.1)
3. Complete the post-2022 sweep; engage the two-dimensional-tableaux line if it
   exists (R2.2) — then delete the "sweep to be completed" disclosure from §9.
4. Author name; abstract trim; strip internal paths from .bib notes; opposite-signs
   sentence; single label on Definition 4.1. (R3.1–3, R1.2, R1.4)
5. Recommended, not required: the inexpressibility paragraph (R2.3).

## Not checked by the panel

PDF compilation (no TeX toolchain available); independent rebuild of the artifact by
Referees 2–3; the primary Arieli–Avron text (paywalled — hence checklist item 2).
